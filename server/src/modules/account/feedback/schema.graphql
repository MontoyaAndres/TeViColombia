type Feedback {
  id: ID
  stars: Int
  comment: String
  user: User
}

type Query {
  feedback(userId: ID!): [Feedback]!
  countFeedbackStars(userId: ID!): Int
}

type Mutation {
  feedback(id: ID!, stars: Int!, comment: String!): [Error!]
  deleteFeedback(id: ID!): Boolean!
}
