type Mutation {
  generalInformationBusiness(
    id: ID
    information: GeneralInformationBusinessInput
  ): [Error!]
}

type Query {
  informationBusiness(id: ID!): BusinessInformation
  memberUser(name: String, lastname: String, email: String): [Customer]
}
